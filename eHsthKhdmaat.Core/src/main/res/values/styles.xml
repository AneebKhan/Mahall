<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Widget.GifView" parent="@android:style/Widget" />

    <style name="IndicatorDialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@color/colorTransparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:statusBarColor" tools:ignore="NewApi">@color/colorTransparent</item>
    </style>

    <style name="datePickerStyle" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/colorPurple</item>
        <item name="android:textColorPrimaryInverse">@color/colorWhite</item>
        <item name="android:colorControlActivated">@color/colorPurple</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColor">#000000</item>

    </style>

</resources>
